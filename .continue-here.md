---
phase: proactive-disclosures
task: 0
total_tasks: 4
status: not_started
last_updated: 2026-04-19T22:20:16Z
---

<current_state>
Site is live at patchdelta.com and www.patchdelta.com. Real CVE data deployed (8 Fortinet, 7 PAN-OS). CI/CD active (push to main auto-deploys via GitHub Actions → Cloudflare Workers).

Proactive disclosure features are fully planned in .planning/PROACTIVE_PLAN.md but not yet implemented. Ready to execute — no blockers.
</current_state>

<completed_work>

- V1 build: React + TypeScript + Vite + Tailwind, 9 commits (7305c56)
- GitHub repo: https://github.com/mikepesh/patchdelta (public)
- Cloudflare Workers Assets deployment + GitHub Actions CI/CD (523724b auto-deploys on push to main)
- Real CVE data populated: 8 Fortinet FortiOS CVEs, 7 PAN-OS CVEs — all from primary sources (523724b)
- www.patchdelta.com DNS A record added (manual, done)
- Memory cleaned: stale V2 entries purged, current V1 state written
- Proactive disclosure plan created: .planning/PROACTIVE_PLAN.md
</completed_work>

<remaining_work>

- T1: Add `proactiveCount: number` to VendorStats interface and computeStats() in src/hooks/useVendorData.ts
- T2A: Render proactive count on Leaderboard cards in src/components/Leaderboard.tsx (conditional, only if > 0)
- T2B: Add "Proactive" badge on negative delta rows + tooltip on Delta column header/cells in src/components/CveTable.tsx
- T3: Expand Methodology.tsx negative delta section — explain coordinated disclosure, cite CVE-2022-42475 (-21 days) as concrete example
</remaining_work>

<decisions_made>

- JSON data in src/data/ with direct TypeScript imports (not public/ + fetch) — spec requires bundled at build time, no runtime API calls
- Cloudflare Workers Assets chosen over Pages — user specified Worker
- Native `title` attribute for tooltip — no new deps, no animations (constraints)
- Border-style badge for "Proactive" label (not filled background) — informative not gamified, security tool aesthetic
- T2A and T2B can execute in parallel after T1; T3 is fully independent
</decisions_made>

<blockers>
None.
</blockers>

<context>
PatchDelta compares vendor patch response times via delta_days (advisory_published - nvd_published). Negative delta = vendor published before NVD = proactive/coordinated disclosure. Fortinet has 5 such CVEs in current data (most dramatic: CVE-2022-42475 at -21 days during active zero-day exploitation). PAN-OS is mostly 0 (same-day coordinated). The 4 features make this signal visible and explained in the UI.
</context>

<next_action>
Run: gsd-executor with .planning/PROACTIVE_PLAN.md — execute T1 first (useVendorData.ts), then T2A + T2B in parallel, then T3. Build and push when all 4 done.
</next_action>
